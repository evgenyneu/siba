source:
  type: files
  include:
    - dir1
    - dir2/subdir2
    - file1
    - /папка/файл

archive:
  type: tar

encryption:
  type: gpg
  passphrase: "this is a passphrase"

destination:
  type: dir
  dir: /dest-dir
