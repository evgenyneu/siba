source:
  type: files
  include:
    - want
    - to
    - one
    - two

archive:
  type: tar

encryption:
  type: gpg
  passphrase: "this is a password"

destination:
  type: dir
  dir: /dir

current_dir: #_replace_current_dir
